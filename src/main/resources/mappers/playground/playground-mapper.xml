<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.multi.ukids.playground.model.mapper.PlaygroundMapper">
	
	<select id="selectPlaygroundList" resultType="Playground" parameterType="map">
		SELECT
			NO, pgid, divL, divM, pgName, pgAddr1, pgAddr2, 
			bName, tel, post, url, lat, lon, water, dc
		FROM PLAYGROUND
		WHERE 1 = 1
		<if test="searchValue != null">
			AND pgName LIKE '%${searchValue}%'
		</if>
		<if test="type != null">
			AND divM LIKE '%${type}%'
		</if>
		<if test="city != null">
			AND pgAddr1 LIKE '%${city}%'
		</if>
		<if test="town != null">
			AND pgAddr1 LIKE '%${town}%'
		</if>
		ORDER BY divM DESC LIMIT 100 OFFSET 0
	</select>

</mapper>